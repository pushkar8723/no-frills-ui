import { Meta } from '@storybook/addon-docs/blocks';
import constants from '../../src/shared/constants';

<Meta title="Get Started/Theming" />

# Theming

I used CSS variables to style each component with a fallback value. What this means is that you can directly use the
component without any theming if you like the blue theme that I followed. Or you can choose to override them by
setting values to each of these variable in `:root` selector.

**Example**

```css
:root {
    --primary: darkorange;
    --primary-light: orange;
}
```

Further, these variables can also be overriden at a block level.

**Example**

```css
.block {
    --primary: darkgreen;
    --primary-light: green;
}
```

You can also style each component by passing a `style` prop or if you like you can set a `className` and then override
style to these class. This should work fine for small tweaks like overriding `width` of the component. But if you want
to do it for a lot of styles, your code quality may take a hit. For such cases refer our
[extending styles guide](/?path=/docs/get-started-extending-styles--page).

> ### CodeSandbox
>
> URL: [https://codesandbox.io/s/theming-etdbq?file=/src/ItemCard.js](https://codesandbox.io/s/theming-etdbq?file=/src/ItemCard.js)
>
> It showcases following features:
> - How easy it is to apply custom theme to the components at various levels.
> - Each component plays nicely with one another and can be customized easily.
> - The DOM is also comparitively clean as only required nodes are mounted.

### CSS Color Variables

<table>
    <thead>
        <tr>
            <th>Variable Name</th>
            <th width="120">Default Value</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><pre>--primary</pre></td>
            <td style={{ backgroundColor: constants.PRIMARY, color: '#fff', textAlign: 'center' }}>{constants.PRIMARY}</td>
        </tr>
        <tr>
            <td><pre>--primary-light</pre></td>
            <td style={{ backgroundColor: constants.PRIMARY_LIGHT, textAlign: 'center' }}>{constants.PRIMARY_LIGHT}</td>
        </tr>
        <tr>
            <td><pre>--primary-lighter</pre></td>
            <td style={{ backgroundColor: constants.PRIMARY_LIGHTER, textAlign: 'center' }}>{constants.PRIMARY_LIGHTER}</td>
        </tr>
        <tr>
            <td><pre>--backdrop-color</pre></td>
            <td style={{ backgroundColor: constants.BACKDROP_COLOR, textAlign: 'center' }}>{constants.BACKDROP_COLOR}</td>
        </tr>
        <tr>
            <td><pre>--info</pre></td>
            <td style={{ backgroundColor: constants.INFO, color: '#fff', textAlign: 'center' }}>{constants.INFO}</td>
        </tr>
        <tr>
            <td><pre>--info-light</pre></td>
            <td style={{ backgroundColor: constants.INFO_LIGHT, textAlign: 'center' }}>{constants.INFO_LIGHT}</td>
        </tr>
        <tr>
            <td><pre>--success</pre></td>
            <td style={{ backgroundColor: constants.SUCCESS, textAlign: 'center' }}>{constants.SUCCESS}</td>
        </tr>
        <tr>
            <td><pre>--success-light</pre></td>
            <td style={{ backgroundColor: constants.SUCCESS_LIGHT, textAlign: 'center' }}>{constants.SUCCESS_LIGHT}</td>
        </tr>
        <tr>
            <td><pre>--warning</pre></td>
            <td style={{ backgroundColor: constants.WARNING, color: '#fff', textAlign: 'center' }}>{constants.WARNING}</td>
        </tr>
        <tr>
            <td><pre>--warning-light</pre></td>
            <td style={{ backgroundColor: constants.WARNING_LIGHT, textAlign: 'center' }}>{constants.WARNING_LIGHT}</td>
        </tr>
        <tr>
            <td><pre>--error</pre></td>
            <td style={{ backgroundColor: constants.ERROR, color: '#fff', textAlign: 'center' }}>{constants.ERROR}</td>
        </tr>
        <tr>
            <td><pre>--error-light</pre></td>
            <td style={{ backgroundColor: constants.ERROR_LIGHT, textAlign: 'center' }}>{constants.ERROR_LIGHT}</td>
        </tr>
        <tr>
            <td><pre>--border-color</pre></td>
            <td style={{ backgroundColor: constants.BORDER_COLOR, color: '#fff', textAlign: 'center' }}>{constants.BORDER_COLOR}</td>
        </tr>
        <tr>
            <td><pre>--toast</pre></td>
            <td style={{ backgroundColor: constants.TOAST, color: '#fff', textAlign: 'center' }}>{constants.TOAST}</td>
        </tr>
        <tr>
            <td><pre>--disabled</pre></td>
            <td style={{ backgroundColor: constants.DISABLED, color : '#fff', textAlign: 'center' }}>{constants.DISABLED}</td>
        </tr>
        <tr>
            <td><pre>--tooltip-color</pre></td>
            <td style={{ backgroundColor: constants.TOOLTIP_COLOR, color : '#fff', textAlign: 'center' }}>{constants.TOOLTIP_COLOR}</td>
        </tr>
        <tr>
            <td><pre>--disabled-border</pre></td>
            <td style={{ backgroundColor: constants.DISABLED_BORDER, textAlign: 'center' }}>{constants.DISABLED_BORDER}</td>
        </tr>
        <tr>
            <td><pre>--light-grey</pre></td>
            <td style={{ backgroundColor: constants.LIGHT_GREY, textAlign: 'center' }}>{constants.LIGHT_GREY}</td>
        </tr>
        <tr>
            <td><pre>--border-light-color</pre></td>
            <td style={{ backgroundColor: constants.BORDER_LIGHT_COLOR, textAlign: 'center' }}>{constants.BORDER_LIGHT_COLOR}</td>
        </tr>
        <tr>
            <td><pre>--disabled-background</pre></td>
            <td style={{ backgroundColor: constants.DISABLED_BACKGROUND, textAlign: 'center' }}>{constants.DISABLED_BACKGROUND}</td>
        </tr>
        <tr>
            <td><pre>--background</pre></td>
            <td style={{ backgroundColor: constants.BACKGROUND, textAlign: 'center' }}>{constants.BACKGROUND}</td>
        </tr>
    </tbody>
</table>

### CSS Shadow Variables

<table>
    <thead>
        <tr>
            <th>Variable Name</th>
            <th>Default Value</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><pre>--shadow</pre></td>
            <td>{constants.SHADOW}</td>
        </tr>
        <tr>
            <td><pre>--hover-shadow</pre></td>
            <td>{constants.HOVER_SHADOW}</td>
        </tr>
        <tr>
            <td><pre>--modal-shadow</pre></td>
            <td>{constants.MODAL_SHADOW}</td>
        </tr>
    </tbody>
</table>
