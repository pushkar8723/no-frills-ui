import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as MenuStories from './Menu.stories';

<Meta of={MenuStories} />

# Menu

A component to render a single or multi-select menu. The value is a string (in case of single select) or
an array (in case of multi-select).

In case there are too many items to be rendered inside a menu, the `Menu` component can contain a virtualizer
like `react-window` which can then render `MenuItem` to ensure performance.

<Canvas of={MenuStories.Variants} className="canvas-background" />

```jsx
import { Menu, MenuItem } from 'no-frills-ui';

//render
<Menu>
    <MenuItem value="apple">Apple</MenuItem>
    <MenuItem value="orange">Orange</MenuItem>
    <MenuItem value="mango">Mango</MenuItem>
    <MenuItem value="Banana">Banana</MenuItem>
</Menu>;
```

## Props of `Menu`

<Controls of={MenuStories.MenuProps} />

## Props of `MenuItem`

<Controls of={MenuStories.MenuItemProps} />

## CSS Variables Used

| Variable Name               | Description                                              |
| :-------------------------- | :------------------------------------------------------- |
| `--nfui-border-light-color` | Used for `border-bottom` and `background-color` (hover). |
| `--nfui-text-color-dark`    | Used for text `color`.                                   |
| `--nfui-text-color-dark`    | Used for `border-color`.                                 |

## Accessibility

### ARIA Roles

- The `Menu` component uses `role="listbox"`.
- Each `MenuItem` uses `role="option"`.

### Keyboard Support

- **ArrowDown**: Move focus to the next item.
- **ArrowUp**: Move focus to the previous item.
- **Home**: Move focus to the first item.
- **End**: Move focus to the last item.
- **Enter / Space**: Select the focused item.
