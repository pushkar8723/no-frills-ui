import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as SpinnerStories from './Spinner.stories';

<Meta of={SpinnerStories} />

# Spinner

Just a rotating spinner. It centers itself automatically.

<Canvas of={SpinnerStories.Sizes} />

```jsx
import { Spinner } from 'no-frills-ui';

// Render
<Spinner />;
```

## Props

<Controls of={SpinnerStories.Primary} />

## CSS Variables Used

| Variable Name               | Description              |
| :-------------------------- | :----------------------- |
| `--nfui-primary`            | Used for `border-color`. |
| `--nfui-border-light-color` | Used for `border-color`. |
