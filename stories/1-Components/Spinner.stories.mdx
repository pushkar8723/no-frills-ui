import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Spinner } from '../../src/components/Spinner';

<Meta title='Declarative Components/Spinner' component={Spinner} />

# Spinner

Just a rotating spinner. It centers it self automatically.

<Preview>
    <Spinner size={40} />
    <Spinner />
    <Spinner size={20} />
    <Spinner size={10} />
</Preview>

```jsx
import { Spinner } from 'no-frills-ui';

// Render
<Spinner />
```

### Props

<Props of={Spinner} />

### CSS Variables Used

| Variable Name | Description |
| :- | :- |
| --primary | Used for `color` and `border-color`. |
