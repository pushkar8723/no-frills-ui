import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Badge, BADGE_TYPE } from '../../src/components/Badge';
import { Button } from '../../src/components/Button';

<Meta title='Declarative Components/Badge' component={Badge} />

# Badge

A styled `div`. This component doesn't do much, but helps maintain consistent expirence across other components.

<Preview style={{ alignItems: 'center' }}>
    <Badge inline type={BADGE_TYPE.PRIMARY}>Primary</Badge>
    <Badge inline type={BADGE_TYPE.SUCCESS}>SUCCESS</Badge>
    <Badge inline type={BADGE_TYPE.WARNING}>Warning</Badge>
    <Badge inline type={BADGE_TYPE.DANGER}>Danger</Badge>
</Preview>

**Overlay:**
<Preview>
    <Button>Filter <Badge>2</Badge></Button>
    <Button>Filter <Badge type={BADGE_TYPE.OVERLAY}>99+</Badge></Button>
    <Button>Filter <Badge type={BADGE_TYPE.OVERLAY}/></Button>
</Preview>

**Inline**
<Preview>
    <Button>Filter <Badge inline>2</Badge></Button>
    <Button>Filter <Badge inline type={BADGE_TYPE.OVERLAY}>99+</Badge></Button>
    <Button>Filter <Badge inline type={BADGE_TYPE.OVERLAY}/></Button>
</Preview>

```jsx
import { Badge } from 'no-frills-ui';

// Render
<Badge />
```
### Props

Following props are added on top of `HTMLSpanElement` props.
<Props of={Badge} />

### CSS Variables Used

| Variable Name | Description |
| :- | :- |
| --primary | Used for `background-color`. |
| --success | Used for `background-color`. |
| --info | Used for `background-color`. |
| --danger | Used for `background-color`. |

