import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Radio } from '../../src/components/Input';

<Meta title='Components/Radio' component={Radio} />

# Radio

Wraps `HTMLInputElement` of type `radio` in a React component to give additional `label` prop.

<Preview>
    <Radio label='Mobile' name='platform' />
    <Radio label='Electron' name='platform' />
    <Radio label='Desktop' name='platform' disabled />
</Preview>

```jsx
import { Radio } from 'no-frills-ui';

// Render
<Radio />
```
### Props
Following props are added on top of `HTMLInputElement` props.
<Props of={Radio} />

### CSS Variables Used

| Variable Name | Description |
| :- | :- |
| --primary | User for `color` and `border-color` and `background-color`. |
| --primary-light | User for `box-shadow`. |
| --background | User for `background` and `box-shadow`. |
| --border-color | User for `border-color`. |
