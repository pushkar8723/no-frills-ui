import { action } from '@storybook/addon-actions';
import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import { Input } from '../../src/components/Input';

<Meta title='Components/Input' component={Input} />

# Input

Wraps `HTMLInputElement` in a React component to give additional `label` and `errorText` props.

A `*` is added to the label if the field is required.

<Preview>
    <form>
        <Input
            label='Name'
            type='text'
            minLength={4}
            style={{ width: '300px' }}
            placeholder='Enter your full name'
            required
        />
        <Input
            label='Email'
            type='email'
            required
        />
        <Input
            label='Website'
            type='url'
        />
        <Input
            label='Country'
            type='text'
            value='India'
            disabled
        />
    </form>
</Preview>

```jsx
import { Input } from 'no-frills-ui';

// Render
<Input />
```
### Props
Following props are added on top of `HTMLInputElement` props.
<Props of={Input} />

### CSS Variables Used

| Variable Name | Description |
| :- | :- |
| --primary | User for `color` and `border-color`. |
| --primary-light | User for `box-shadow`. |
| --error | User for `color` and `border-color`. |
| --error-light | User for `box-shadow`. |
| --background | User for `background-color`. |
| --border-color | User for `border-color`. |
